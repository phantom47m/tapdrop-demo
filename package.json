{
  "name": "tapdrop",
  "version": "0.1.0",
  "private": true,
  "description": "TapDrop — file handoff via QR + short code. Static demo (47 Industries × iUSEJOE).",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
